import { CountryFlagPathPipe } from './country-flag-path.pipe';

describe('CountryFlagPathPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryFlagPathPipe();
    expect(pipe).toBeTruthy();
  });
});
